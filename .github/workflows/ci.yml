name: CD for my pipeline :)

on: 
  push:
    branches: [ main ]
  
  workflow_dispatch:

jobs:
  deploy_code:
    name: Deploy my Great Databricks Pipeline to production
    uses: singhanat/test/.github/workflows/reusable_databricks_code_deployment.yml@v4.0.0
    with:
      databricks_code_path: "/Workspace/Users/singhanat.rer@kmutt.ac.th"
      service_code_file: "main.py"
    secrets:
      DATABRICKS_HOST_URL: ${{ secrets.MY_DATABRICKS_HOST }}
      DATABRICKS_TOKEN: ${{ secrets.MY_DATABRICKS_TOKEN }}
